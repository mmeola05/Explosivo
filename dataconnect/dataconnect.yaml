specVersion: "v1beta"
serviceId: "Quedadas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Quedadas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
